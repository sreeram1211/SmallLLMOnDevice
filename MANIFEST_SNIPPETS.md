# AndroidManifest snippets (PocketSloth)

These were **merged** into `app/src/main/AndroidManifest.xml` by the
Data/Domain/Service agent:

- `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_SPECIAL_USE`,
  `FOREGROUND_SERVICE_DATA_SYNC`, `WAKE_LOCK`, `POST_NOTIFICATIONS`
- `<service android:name=".service.FineTuningService" …>`

If a later merge drops them, re-apply from the current manifest or the
blocks previously documented here.
